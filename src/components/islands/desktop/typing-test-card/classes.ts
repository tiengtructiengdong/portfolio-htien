/**
 * classes.ts
 * Tailwind class constants specific to the TypingTestCard component.
 */

/** Muted mono label (e.g. dates, categories). */
export const MUTED_MONO = "font-mono text-sm text-[var(--color-fg-muted)]";
